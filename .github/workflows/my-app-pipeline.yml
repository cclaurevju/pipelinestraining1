name: CI

on:
  push:
    branches: ["carlosClaure_p1_dev", "main"]
  pull_request:
    branches: ["carlosClaure_p1_dev", "main"]

  workflow_dispatch:

jobs:
  check-code:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

  build:
    runs-on: self-hosted
    steps:
      - name: Building project
        run: dotnet build
